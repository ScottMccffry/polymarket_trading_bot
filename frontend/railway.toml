[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
# Pass env vars as build args for Next.js
buildArgs = ["NEXT_PUBLIC_API_URL", "NEXT_PUBLIC_WS_URL"]

[deploy]
numReplicas = 1
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
